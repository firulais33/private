<body class="Total" >
  <div class="col-md-6 col-md-offset-3">
    <div class="login-form">
      <% if admin_signed_in? or @comentario.user_id == current_user.id %>
        <%= simple_form_for ([@restaurant, @comentario]) do |f|  %>
          <%= f.input :body, label: "Comentario" %>
          <%= f.input :valoracion %>
          <%= f.button :submit %>
        <% end %>
      <% end %>
    </div>
  </div>
</body>
